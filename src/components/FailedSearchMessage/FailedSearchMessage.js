
const FailedSearchMessage = ({message}) => {

    return (
        <section
            className="failed-search-message" 
        >
            <span>{message}</span>
        </section>
    )
}

export default FailedSearchMessage;