import '../UserReposSection/UserReposSection.scss';

const UserReposSection = () => {

    return (
        <section 
            className="user-repos-section"
            data-testid="user-repos-section"
        >
            
        </section>
    )
}

export default UserReposSection;